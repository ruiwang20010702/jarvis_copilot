# 🎯 Classroom.tsx 重构指南

## ✅ 已完成的工作

### 1. 已创建的模块 (4/6)

| 模块 | 位置 | 状态 |
|------|------|------|
| Warmup | `src/components/stages/Warmup/` | ✅ 完成 |
| Skill | `src/components/stages/Skill/` | ✅ 完成 |
| Vocab | `src/components/stages/Vocab/` | ✅ 完成 |
| Surgery | `src/components/stages/Surgery/` | ✅ 完成 |

### 2. 已添加的导入

```typescript
// 在 Classroom.tsx 第 8-11 行已添加
import { StudentWarmupView, CoachWarmupView } from '../src/components/stages/Warmup';
import { StudentSkillView, CoachSkillView } from '../src/components/stages/Skill';
import { StudentVocabView, CoachVocabView } from '../src/components/stages/Vocab';
import { StudentSurgeryView, CoachSurgeryView } from '../src/components/stages/Surgery';
```

### 3. 已更新的渲染逻辑

- `StudentView` 组件 (252-278行) - ✅ 已更新
- `CoachView` 组件 (3305-3319行) - ✅ 已更新

## ⚠️ 需要手动完成的操作

### 删除旧组件定义

**重要**: 以下组件已在新模块中，需要从 Classroom.tsx 删除以消除冲突：

| 组件 | 起始行 | 结束行 | 代码量 |
|------|--------|--------|--------|
| `TEXT_RAIN_WORDS` 常量 | 292 | 299 | 8行 |
| `TextRainParticle` 组件 | 302 | 324 | 23行 |
| `GPSEquipCard` 组件 | 326 | 488 | 163行 |
| `InteractiveDemo` 组件 | 490 | 609 | 120行 |
| `StudentToolbar` 组件 | 611 | 683 | 73行 |
| `SKILL_QUIZ_DATA` 常量 | 685 | 699 | 15行 |
| `StudentSkillView` | 701 | 1263 | 563行 |
| `StudentWarmupView` | 1265 | 1472 | 208行 |
| `StudentSurgeryView` | 3181 | 3293 | 113行 |
| `CoachSkillView` | 3363 | 3937 | 575行 |
| `CoachWarmupView` | 3939 | 4154 | 216行 |
| `CoachVocabView` | 5012 | 5384 | 373行 |
| `CoachSurgeryView` | 5390 | 5784 | 395行 |

**删除方法**:
1. 手动选择这些行范围
2. 删除内容
3. 保存文件

**预期结果**:
- Classroom.tsx 将减少 **约2845行代码**
- 从 5787行 → 约2942行
- Linter错误将消失

## 🚧 待提取的模块 (2/6)

由于这两个模块极其复杂，建议单独处理：

### Battle (实战)

**复杂度**: ⭐⭐⭐⭐⭐ (最高)

**提取内容**:
- `StudentBattleView` (1474-2289行, ~815行)
- `ChatPanel` 组件 (2292-2379行, ~88行)
- `CoachBattleView` (4156-4680行, ~525行)

**总计**: ~1428行

**关键功能**:
- 文本选择和高亮系统
- 查词功能（带次数限制和动画）
- 答题系统（确定/不确定标记）
- 实时监控数据
- 活动日志系统

### Coaching (带练)

**复杂度**: ⭐⭐⭐⭐⭐ (最高)

**提取内容**:
- `StudentCoachingView` (2381-2680行, ~300行)
- `CoachCoachingView` (4682-5010行, ~329行)

**总计**: ~629行

**关键功能**:
- 任务分发系统
- 文本聚焦效果
- 学生互动监控
- Jarvis 建议引擎

## 📋 提取步骤建议

### 方案A: 简化方案（推荐）

暂时保留 Battle 和 Coaching 在 Classroom.tsx 中，只删除已提取的4个模块的旧定义。

**优点**: 
- 快速完成当前重构
- 降低出错风险
- 50%的代码已经模块化

**操作**:
1. 删除上述列表中的旧组件（2845行）
2. 测试功能
3. 稍后再提取 Battle 和 Coaching

### 方案B: 完整方案

继续提取 Battle 和 Coaching。

**操作**:
1. 创建 `src/components/stages/Battle/` 目录
2. 提取组件代码
3. 创建 `src/components/stages/Coaching/` 目录  
4. 提取组件代码
5. 更新 Classroom.tsx 导入

## 🧪 测试检查清单

重构完成后，请测试以下功能：

### 热身阶段 (Warmup)
- [ ] 学生端：聊天输入和快速回复
- [ ] 教师端：Jarvis建议和开场白按钮

### 技能阶段 (Skill)
- [ ] 学生端：文字雨动画
- [ ] 学生端：GPS卡片装备
- [ ] 学生端：Mini-Quiz
- [ ] 教师端：横向时间轴
- [ ] 教师端：实时监控卡片

### 生词阶段 (Vocab)
- [ ] 学生端：单词卡片翻转
- [ ] 学生端：出口测试
- [ ] 教师端：控制面板

### 难句阶段 (Surgery)
- [ ] 学生端：句子拆解
- [ ] 教师端：模式切换

## 🎉 预期成果

完成后的 Classroom.tsx 应该：
- ✅ 只包含核心布局和路由逻辑
- ✅ 所有阶段组件通过导入使用
- ✅ 代码量减少50%+
- ✅ 更易于维护和扩展

